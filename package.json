{
  "name": "sistema-loja-backend",
  "version": "1.0.0",
  "main": "src/server.js",  // ← Adiciona src/
  "scripts": {
    "start": "node src/server.js",  // ← Adiciona src/
    "dev": "nodemon src/server.js",
    "build": "prisma generate",
    "migrate": "prisma migrate deploy",
    "seed": "node src/seed.js",  // ← Adiciona src/
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0"
  }
}